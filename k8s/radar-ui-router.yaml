apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radar-ui-route
  namespace: radar
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik-system
  hostnames:
    - "radar.local"
  rules:
    - backendRefs:
        - name: radar
          port: 9280